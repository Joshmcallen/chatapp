extends layout

block content
  h1 Create a new chat room

  form(method = "post")
    fieldset.form-group
      input.form-control(name = "name", type='text', placeholder = "Enter a name")
    button.btn.btn-primary(type="submit") Create New Chat
    a.btn.btn-default(href="/admin/rooms") Cancel
